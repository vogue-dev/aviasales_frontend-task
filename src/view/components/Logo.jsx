import React from 'react';
import logo from '../../logo.svg';

const Logo = () => {
	return (
		<div className="logo">
			<img src={logo} className="app-logo" alt="logo" />
		</div>
	);
};

export default Logo;
